import 'package:flutter/material.dart';

class SafeGateScreen extends StatelessWidget {
  const SafeGateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('البوابة الآمنة')),
      body: const Center(child: Text('SafeGate Screen - To be implemented')),
    );
  }
}
